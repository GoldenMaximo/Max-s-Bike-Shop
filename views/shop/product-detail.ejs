<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
        <main class="centered">
            <h1><%= product.title %></h1>
            <div class="image">
                <img src="/<%= product.imageUrl %>" alt="product image">
            </div>
            <h2><%= product.price %></h2>
            <p class="product__detail_description"><%= product.description %></p>
            <table class="product__detail_table">
                <tr>
                    <td>Weight: <%= product.weight %></td>
                </tr>
            </table>
            <% if (isAuthenticated) { %>
                <%- include('../includes/add-to-cart.ejs', {product}) %>
            <% } %>
        </main>
        <%- include('../includes/end.ejs') %>